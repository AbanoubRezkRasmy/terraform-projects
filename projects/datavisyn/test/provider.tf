/* -------------------------------------------------------------------------- */
provider "aws" {
  region  = "eu-west-1"
  assume_role {
    role_arn = "arn:aws:iam::297071026656:role/terraform-role"
  }
}
/* -------------------------------------------------------------------------- */
provider "helm" {
  kubernetes {
    config_path = "~/.kube/config"
  }
}
/* -------------------------------------------------------------------------- */
provider "kubernetes" {
  config_path = "~/.kube/config"
}
/* -------------------------------------------------------------------------- */
provider "time" {}
